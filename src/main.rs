extern crate minimal;
use minimal::{ MinimalDocument, MinimalElement, MinimalHtml, MinimalList, MinimalWindow};
fn main(){
    let window = minimal::window();
    let document = minimal::document();
}